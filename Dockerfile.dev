# Dockerfile.dev
FROM node:18-alpine

# Install pnpm globally
RUN npm install -g pnpm

# Set working directory inside container
WORKDIR /app

# Use a startup command that installs dependencies and then starts the watch mode.
# (The bind mount will supply your source code.)
CMD ["sh", "-c", "pnpm install && pnpm run dev"]
