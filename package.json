{
  "source": "src/main.js",
  "targets": {
    "default": {
      "distDir": "./dist"
    }
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "dev": "vite && cp src/manifest.json dist//",
    "build": "vite build && cp src/manifest.json dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.252",
    "@types/node": "^20.9.3",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "chart.js": "^4.3.0"
  }
}